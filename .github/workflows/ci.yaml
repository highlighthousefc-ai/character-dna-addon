name: CI
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

env:
  RUFF_VERSION: "0.15.8"

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: astral-sh/setup-uv@v7

      # Upstream code has known findings, so they are reported but don't fail the job yet.
      - name: Ruff (inherited code, report only)
        run: uvx ruff@${RUFF_VERSION} check --exit-zero src tests scripts

      - name: Ruff format
        run: uvx ruff@${RUFF_VERSION} format --check src/addons/character_dna scripts/ci

  register:
    name: Register (${{ matrix.os }}, bpy ${{ matrix.bpy }})
    needs: [lint]
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, macos-latest]
        bpy: ["5.1.*", "5.2.*"]
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Install bpy
        run: python -m pip install "bpy==${{ matrix.bpy }}"

      - name: Register and unregister the addon
        run: python scripts/ci/register_smoke.py
